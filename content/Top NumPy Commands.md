---
tags:
  - type/pillar
project: "[[Neural Networks]]"
publish: true
---

### Array Creation
1. `np.array(obj, dtype, copy)`
2. `np.zeros(shape)`
3. `np.ones(shape)`
4. `np.arange(start, stop, step)`
5. `np.linspace(start, stop, num)`
6. `np.eye(N)`
7. `np.full(shape, fill_value)`

### Reshaping
8. `np.reshape(a, newshape)`
9. `np.flatten()`
10. `np.transpose(a)`
11. `np.concatenate()`
12. `np.stack()`
13. `np.split()`
14. `np.expand_dims()`

### Math
15. `np.sum()`, `np.mean()`, `np.std()`
16-21. `exp`, `log`, `sqrt`, `clip`, etc.

### Linear Algebra (`np.linalg`)
22-27. `dot`, `matmul`, `inv`, `norm`, `eig`, `solve`

### Search/Filter
28. `np.where()`
29. `np.argmax()`
30. `np.unique()`